<%= render 'bootstrap_costumes/cards/tabbed_card', tabs: @page.children_tabs, options: pages__options(@page) %>

<p>
  <strong>Name:</strong>
  <%= @page.name %>
</p>

<p>
  <strong>Page:</strong>
  <%= @page.page_id %>
</p>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
